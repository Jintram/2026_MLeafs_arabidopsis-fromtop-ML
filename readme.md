


### Prerequisites

Make sure that https://github.com/Jintram/Cheeky-cells is installed.

### How to convert tif files to npy files for training

1. Use `helpers/convert_tifs_to_npy.py` to convert to npy.
2. Make sure that the filename is `<original-name>_tile_seg.npy`.